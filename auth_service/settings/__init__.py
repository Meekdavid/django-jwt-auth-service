# Settings module selector
# Use DJANGO_SETTINGS_MODULE environment variable to determine which settings to load
# This file should remain empty to allow proper settings selection via environment variables